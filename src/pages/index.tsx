import { Heading } from "@chakra-ui/react";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <>
      <Heading fontSize={500}>Hello</Heading>
      <Heading fontSize={500}>Hello</Heading>
    </>
  );
};

export default Home;
